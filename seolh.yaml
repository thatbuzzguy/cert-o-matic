client_cert_config:
  algorithm_name: secp256r1
  certificate_lifetime_in_days: 1
  city_or_locality: Pullman
  country_name: US
  email_address: user@localhost
  hash_name: sha512
  organization: Flying Circus
  organizational_unit: Elephant Wrangler Union
  state_or_province: WA
self_signed: true
service_config:
  algorithm_name: secp256r1
  auth_psk: null
  backend: default_backend
  certificate_lifetime_in_days: 30
  city_or_locality: Pullman
  common_name: seolhic test ca
  config_file: seolh.yaml
  country_name: US
  database: ca.db
  email_address: root@localhost
  fqdn: localhost
  hash_name: sha512
  ip_address: 127.0.0.1
  issuer_name: seolhic test ca
  organization: Flying Circus
  organizational_unit: Elephant Wrangler Union
  port_number: 80
  private_key_file: root_private_key.der
  private_key_format: der
  private_key_password: null
  root_certificate_file_name: root_cert.der
  root_certificate_format: der
  state_or_province: WA
  subject_alternate_names: localhost
