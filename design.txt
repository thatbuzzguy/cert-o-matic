Program Flow

Initialize
1 - Check for base configuration
	If base config is present bootstrap system 
	Else wait for initial configuration
2 - Pre-flight check 
	CA certificate is valid (not revoked, not expired)
		Not valid ->  reissue
	CA certificate is appropriate for CA settings (certificate matches DN, usage, etc.)
		
	CA certificate matches configured CA relationships (self-signed, signed by parent, cross-signed)
	Certificate Database is present 
	Certificate Database is consistant with settings
	
3 - 









Persistant Data Storage in Yaml:

--root CA data - CA-data.yaml --
common name
organization
organizational unit
state or province
Country Name
city or cocality
signature algorithm
signature hash algorithm
certificate lifetime
certificate version
thumbprint algorithm
email address

--root CA certificate data - CA-cert-db.yaml --
private key text
public key text
certificate text

--certificate template database - CA-template-db.yaml --
template name 
template number
template serial 

--certificate database - CA-certificate-db.yaml --
certificate common name
certificate 

--client certificate template data - template[number].yaml --
template name
template number
organization
organizational unit
locality
state or province
country or region
signature algorithm
signature hash algorithm
certificate lifetime
certificate version
thumbprint algorithm



